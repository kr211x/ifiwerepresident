  <p style="margin-bottom:10px;">Filter By Tags</p>
  
  <% current_names = params[:tags].split(',') rescue [] %>
  <% @issue.tags.each do |t| %>
    <% if current_names.include?(t.name) %>
      <%= tag_link t, true %>
    <% else %>
      <%= tag_link t %>
    <% end %>
  <% end %>
